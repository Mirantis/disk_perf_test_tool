include: default.yaml
collect_info: false

#ceph:
#    root_node: localhost

nodes:
    koder@localhost: testnode

tests:
    - io:
        load: rrd
        params:
            FILENAME: /tmp/fl.bin
            FILESIZE: 16M

